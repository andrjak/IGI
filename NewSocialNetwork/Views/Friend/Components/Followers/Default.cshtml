@if (Model != null && Model.Count != 0)
{
    <h2>Followers</h2>
    @foreach (User item in Model)
    {
        <div>
            <img src=@item.ImagePath alt="User photo" width="300" height="250" />
            <h4>@item.Name @item.Surname</h4>
            <p>@item.UserName</p>
        </div>
    }

}
else
{
    <h4> Followers list is empty </h4>
}
