@if (Model != null && Model.Count != 0)
{
    <h2 class="text-center">Followers</h2>
    @foreach (User item in Model)
    {
        @await Html.PartialAsync("_Follower", item)
        <hr />
    }

}
else
{
    <h4 class="text-center"> Followers list is empty </h4>
}
